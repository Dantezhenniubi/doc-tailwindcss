---
outline: [2, 6]
tags: ['css', '语法', '前端']
---


# CSS选择器


## :focus-within
:focus-within 伪类选择器在元素及其后代元素中匹配获得焦点的状态。

很适用于以下场景：

你在一个输入框元素包裹了一层装饰容器，输入框获得焦点时，你需要让这个装饰容器也获得焦点状态，但是本身装饰容器没有焦点状态，这时候就可以在装饰容器使用 :focus-within 选择器获取子元素输入框的焦点状态，从而实现装饰容器也获得焦点状态的效果。